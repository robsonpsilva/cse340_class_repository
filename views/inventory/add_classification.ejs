<!-- Creating the content heading -->
 <% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>
<!-- The next code shows a message on the screen -->
 <%- messages() %>
<!-- Here we are showing the view main content -->
 <div class = "external">
  <div class="form-container">
    <h2>FIELD IS REQUIRED</h2>
    <form id="vehicleForm" name="vehicleForm" action = "/inv/add_classification" method = "post" class="login-form">
      <h3>Classification Name</h3>

      <!-- Make -->
      <label for="classification_name">NAME MUST ALPHABETIC CHARACTERS ONLY.</label>
      <input type="text" id="classification_name" name="classification_name" required placeholder="Enter the classification" value="<%= locals.classification_name %>" pattern="[a-zA-Z]*$">
      <!-- Submit Button -->
      <button type="submit">Add Classification</button>
    </form>
  </div>
</div>